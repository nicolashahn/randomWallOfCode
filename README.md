# randomWallOfCode
Generates a random wall of code, like you see on any major news out's articles on software.

Usage:
```
python3 randomWallOfCode.py <width> <height> <output_file>
```

A sample (80 by 40 characters):

```
str(lineNo)if (i > 0){<div>}</a>SELECT * FROM posts; wget "http://www.fcc.gov/fi
} else {SELECT * FROM posts; str(lineNo)fib :: Int -> Int int main(int argc, cha
</a>return result; mkdir logs cat names | grep "Rob"str(lineNo))};public void ma
)};if (i > 0){while(True){int main(int argc, char *argv[]){ wget "http://www.fcc
(int) sc.readLine(); public void main (String args[]){fib :: Int -> Int SELECT *
return result; <a href="http://github.com">}pip install num (int) sc.readLine();
</a>for (i:strings){while(True){(int) sc.readLine(); fib :: Int -> Int )};if (i 
pip install numpyimport java.util.*j += 3; class BTree(Object): h.toString();str
j += 3; public void main (String args[]){int main(int argc, char *argv[]){ <div>
import java.util.*<tr>public void main (String args[]){pip install numpyh.toStri
while(True){<a href="http://github.com">mkdir logs j += 3; (int) sc.readLine(); 
cat names | grep "Rob"pip install numpySELECT * FROM posts; h.toString();<tr>} e
println('error'); if (i > 0){int main(int argc, char *argv[]){ while(True){j += 
wget "http://www.fcc.gov/filings"mkdir logs </div>fib :: Int -> Int <tr>bdict = 
<div>j += 3; cat names | grep "Rob"fib :: Int -> Int <a href="http://github.com"
pip install numpy(int) sc.readLine(); int main(int argc, char *argv[]){ } else {
import java.util.*</tr>bdict = {} if (i > 0){wget "http://www.fcc.gov/filings"cl
</a>cat names | grep "Rob"import java.util.*j += 3; </div>mkdir logs question = 
<a href="http://github.com">)};pip install numpybdict = {} </a>if (i > 0){questi
<a href="http://github.com">)};while(True){int main(int argc, char *argv[]){ fib
while(True){import java.util.*$('body').html('login failed'); class BTree(Object
<div>} else {return result; cat names | grep "Rob"question = r"(.+\?)"wget "http
j += 3; if (i > 0){import java.util.*</tr></div>}question = r"(.+\?)"h.toString(
import java.util.*while(True){fib :: Int -> Int </div>question = r"(.+\?)"h.toSt
<div>cat names | grep "Rob"int main(int argc, char *argv[]){ } else {bdict = {} 
bdict = {} return result; <div></tr>fib :: Int -> Int $('body').html('login fail
}return result; while(True){(int) sc.readLine(); </a>bdict = {} <a href="http://
return result; $('body').html('login failed'); )};(int) sc.readLine(); <tr>h.toS
println('error'); </div>h.toString();(int) sc.readLine(); while(True){SELECT * F
while(True){str(lineNo)bdict = {} h.toString();SELECT * FROM posts; println('err
} else {wget "http://www.fcc.gov/filings"class BTree(Object): fib :: Int -> Int 
int main(int argc, char *argv[]){ }str(lineNo)</a>pip install numpySELECT * FROM
<a href="http://github.com"></tr>import java.util.*question = r"(.+\?)"for (i:st
)};</tr><a href="http://github.com">bdict = {} for (i:strings){public void main 
h.toString();<div>bdict = {} </tr><a href="http://github.com">pip install numpyi
cat names | grep "Rob"j += 3; fib :: Int -> Int } else {<tr>import java.util.*pu
bdict = {} println('error'); <a href="http://github.com">str(lineNo)<tr>}import 
public void main (String args[]){h.toString();bdict = {} }<div>} else {cat names
import java.util.*j += 3; <a href="http://github.com">$('body').html('login fail
h.toString();pip install numpyfor (i:strings){$('body').html('login failed'); ca
```
