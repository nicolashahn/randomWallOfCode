# randomWallOfCode
Generates a random wall of code, like you see on any major news outlet's articles on software, or 'hacking.'

Usage:
```
python3 randomWallOfCode.py <width> <height> <output_file>
```

A sample (80 by 40 characters):

```
(int) sc.readLine(); cat names | grep "Rob" <tr>int main(int argc, char *argv[])
j += 3; while(True){fib :: Int -> Int return result; wget "http://www.fcc.gov/fi
mkdir logs wget "http://www.fcc.gov/filings"if (i > 0){cat names | grep "Rob" <d
<tr>pip install numpy </div>println('error'); $('body').html('login failed'); mk
int main(int argc, char *argv[]){ cat names | grep "Rob" if (i > 0){import java.
return result; question = r"(.+\?)" while(True){if (i > 0){h.toString();cat name
j += 3; SELECT * FROM posts; public void main (String args[]){bdict = {} fib :: 
SELECT * FROM posts; question = r"(.+\?)" class BTree(Object): }println('error')
} else {}str(lineNo) (int) sc.readLine(); </a>$('body').html('login failed'); fo
<tr><a href="http://github.com">} else {</tr>println('error'); bdict = {} pip in
wget "http://www.fcc.gov/filings"if (i > 0){public void main (String args[]){pip
class BTree(Object): int main(int argc, char *argv[]){ return result; for (i:str
for (i:strings){cat names | grep "Rob" h.toString();while(True){if (i > 0){retur
</a>question = r"(.+\?)" SELECT * FROM posts; public void main (String args[]){f
return result; import java.util.* <a href="http://github.com">} else {for (i:str
int main(int argc, char *argv[]){ while(True){<div>bdict = {} **kwargs } else {q
</a>)};return result; mkdir logs class BTree(Object): cat names | grep "Rob" </d
</div>int main(int argc, char *argv[]){ h.toString();class BTree(Object): for (i
(int) sc.readLine(); $('body').html('login failed'); wget "http://www.fcc.gov/fi
question = r"(.+\?)" fib :: Int -> Int class BTree(Object): **kwargs cat names |
while(True){} else {mkdir logs bdict = {} SELECT * FROM posts; **kwargs j += 3; 
} else {wget "http://www.fcc.gov/filings"int main(int argc, char *argv[]){ while
int main(int argc, char *argv[]){ for (i:strings){SELECT * FROM posts; **kwargs 
h.toString();</tr>fib :: Int -> Int if (i > 0){**kwargs class BTree(Object): ret
</a><a href="http://github.com">while(True){cat names | grep "Rob" mkdir logs } 
class BTree(Object): wget "http://www.fcc.gov/filings"cat names | grep "Rob" SEL
$('body').html('login failed'); mkdir logs str(lineNo) h.toString();} else {fib 
if (i > 0){pip install numpy fib :: Int -> Int </a><tr>)};SELECT * FROM posts; w
int main(int argc, char *argv[]){ h.toString();bdict = {} j += 3; mkdir logs <tr
<a href="http://github.com">wget "http://www.fcc.gov/filings")};if (i > 0){class
**kwargs class BTree(Object): if (i > 0){fib :: Int -> Int SELECT * FROM posts; 
pip install numpy **kwargs while(True){mkdir logs for (i:strings){<div>class BTr
)};}mkdir logs question = r"(.+\?)" import java.util.* <a href="http://github.co
int main(int argc, char *argv[]){ return result; } else {$('body').html('login f
$('body').html('login failed'); cat names | grep "Rob" j += 3; pip install numpy
h.toString();if (i > 0){j += 3; <div>(int) sc.readLine(); mkdir logs )};int main
if (i > 0){</tr>int main(int argc, char *argv[]){ fib :: Int -> Int cat names | 
<tr>mkdir logs while(True){j += 3; </div></tr>for (i:strings){fib :: Int -> Int 
str(lineNo) </a>h.toString();int main(int argc, char *argv[]){ class BTree(Objec
</div>)};bdict = {} class BTree(Object): <a href="http://github.com"></a>cat nam
```
